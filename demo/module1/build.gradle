dependencies {
    if (project.ext.isPublish) {
        implementation "com.lkl.flowcontrol:appinit-test-module3:${rootProject.project(':module3').POM_VERSION_NAME}"
    } else {
        implementation project(':module3')
    }
}